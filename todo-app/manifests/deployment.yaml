apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: shared-image
          persistentVolumeClaim:
            claimName: todo-pvc
      containers:
        - name: todo-app
          image: march787/todo-app:v10
          volumeMounts:
            - name: shared-image
              mountPath: /usr/src/app/files
          env:
            - name: PORT
              value: '3000'
            - name: OUTPUT_FILE_PATH
              value: "/usr/src/app/files/"
            - name: NEXT_PUBLIC_BACKEND_URL
              value: "http://localhost:8081/todo-backend/"
        